import { Save, ShieldCheck } from "lucide-react";
export default function SecuritySection({ values, onChange, onSave, saving }) { return <section className="settings-panel"><header className="settings-panel-header"><span><ShieldCheck size={19} /></span><div><h2>Security</h2><p>Configure account protection and session behavior</p></div></header><div className="settings-panel-body"><div className="settings-toggle-row"><div><strong>Require Strong Passwords</strong><p>Require stronger passwords for staff accounts</p></div><button type="button" role="switch" aria-label="Require Strong Passwords" aria-checked={values.require_strong_password} className={`settings-switch ${values.require_strong_password ? "active" : ""}`} onClick={() => onChange("require_strong_password", !values.require_strong_password)}><span /></button></div><label>Session Timeout<div className="settings-unit-input"><input aria-label="Session Timeout" type="number" min="5" max="1440" value={values.session_timeout_minutes} onChange={(event) => onChange("session_timeout_minutes", Number(event.target.value))} /><span>min</span></div></label></div><footer className="settings-panel-footer"><button type="button" className="btn btn-primary" disabled={saving} onClick={onSave}><Save size={16} /> Save Security</button></footer></section>; }
