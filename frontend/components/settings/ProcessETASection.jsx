import { Clock3, Save } from "lucide-react";
const EtaInput = ({ label, value, onChange }) => <label>{label}<div className="settings-unit-input"><input aria-label={`${label} time`} type="number" min="1" max="1440" value={value} onChange={(event) => onChange(Number(event.target.value))} /><span>min</span></div></label>;
export default function ProcessETASection({ values, onChange, onSave, saving }) { const total = Number(values.washing_minutes || 0) + Number(values.drying_minutes || 0) + Number(values.folding_minutes || 0); return <section className="settings-panel eta-panel"><header className="settings-panel-header"><span><Clock3 size={19} /></span><div><h2>Process ETA</h2><p>Configure the expected duration of each laundry stage</p></div></header><div className="settings-panel-body"><div className="settings-input-grid three"><EtaInput label="Washing" value={values.washing_minutes} onChange={(value) => onChange("washing_minutes", value)} /><EtaInput label="Drying" value={values.drying_minutes} onChange={(value) => onChange("drying_minutes", value)} /><EtaInput label="Folding" value={values.folding_minutes} onChange={(value) => onChange("folding_minutes", value)} /></div><div className="settings-total-eta"><span>Total ETA</span><strong>{total} min</strong><small>{Math.floor(total / 60) ? `${Math.floor(total / 60)} hr ` : ""}{total % 60 ? `${total % 60} min` : ""}</small></div></div><footer className="settings-panel-footer"><button type="button" className="btn btn-primary" disabled={saving} onClick={onSave}><Save size={16} /> Save ETA Settings</button></footer></section>; }
