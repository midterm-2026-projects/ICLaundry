import { Bell, Moon } from "lucide-react";
const Toggle = ({ label, description, checked, onChange }) => <div className="settings-toggle-row"><div><strong>{label}</strong><p>{description}</p></div><button type="button" role="switch" aria-label={label} aria-checked={checked} className={`settings-switch ${checked ? "active" : ""}`} onClick={() => onChange(!checked)}><span /></button></div>;
export default function PreferenceSection({ darkMode, notifications, onDarkModeChange, onNotificationsChange }) { return <div className="settings-preference-grid"><section className="settings-panel compact"><header className="settings-panel-header"><span><Moon size={19} /></span><div><h2>Appearance</h2><p>Customize how the application looks</p></div></header><div className="settings-panel-body"><Toggle label="Dark Mode" description="Use a darker color palette across the system" checked={darkMode} onChange={onDarkModeChange} /></div></section><section className="settings-panel compact"><header className="settings-panel-header"><span><Bell size={19} /></span><div><h2>Notifications</h2><p>Control operational update alerts</p></div></header><div className="settings-panel-body"><Toggle label="Enable Notifications" description="Receive order, inventory, and DSS alerts" checked={notifications} onChange={onNotificationsChange} /></div></section></div>; }
