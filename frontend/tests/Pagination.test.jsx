import{fireEvent,render,screen}from"@testing-library/react";import{expect,it,vi}from"vitest";import Pagination from"../components/Pagination";it("renders page information and invokes navigation",()=>{const next=vi.fn();const{container}=render(<Pagination currentPage={1} totalPages={5} onNext={next}/>);expect(container.querySelector(".inventory-pagination-info")).toHaveTextContent("Page 1 of 5");fireEvent.click(screen.getByRole("button",{name:"Next Page"}));expect(next).toHaveBeenCalledOnce();expect(screen.getByRole("button",{name:"Previous Page"})).toBeDisabled();});
