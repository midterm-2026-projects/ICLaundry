import{fireEvent,render,screen}from"@testing-library/react";import{expect,it,vi}from"vitest";import AddItemModal from"../components/AddItemModal";it("does not render while closed",()=>{render(<AddItemModal isOpen={false}/>);expect(screen.queryByRole("heading")).not.toBeInTheDocument();});it("renders and validates",()=>{render(<AddItemModal isOpen onSaveItem={vi.fn()}/>);expect(screen.getByRole("heading",{name:"Add Inventory Item"})).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Save Item"}));expect(screen.getByRole("alert")).toHaveTextContent(/item name is required/i);});
