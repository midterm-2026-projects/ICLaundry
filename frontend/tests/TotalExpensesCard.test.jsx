import{render,screen}from"@testing-library/react";import{expect,it}from"vitest";import TotalExpensesCard from"../components/TotalExpensesCard";it("formats numeric expenses",()=>{render(<TotalExpensesCard value={10000}/>);expect(screen.getByText("₱10,000.00")).toBeInTheDocument();});it("defaults to zero",()=>{render(<TotalExpensesCard/>);expect(screen.getByText("₱0.00")).toBeInTheDocument();});
