import{fireEvent,render,screen}from"@testing-library/react";import{expect,it,vi}from"vitest";import AddItemButton from"../components/AddItemButton";it("renders and invokes add",()=>{const add=vi.fn();render(<AddItemButton onAddItem={add}/>);fireEvent.click(screen.getByRole("button",{name:"Add Inventory Item"}));expect(add).toHaveBeenCalledOnce();});it("supports disabled state",()=>{render(<AddItemButton disabled/>);expect(screen.getByRole("button")).toBeDisabled();});
