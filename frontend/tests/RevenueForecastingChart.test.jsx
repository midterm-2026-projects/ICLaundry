import{render,screen}from"@testing-library/react";import{expect,it}from"vitest";import RevenueForecastingChart from"../components/RevenueForecastingChart";
it("renders numeric forecast data",()=>{render(<RevenueForecastingChart title="Revenue Forecast" data={[{period:"Jul",forecast:50000}]} valueKey="forecast"/>);expect(screen.getByRole("img",{name:/bar chart/i})).toBeInTheDocument();expect(screen.getByText("₱50,000")).toBeInTheDocument();expect(screen.getByText("Jul")).toBeInTheDocument();});it("renders an empty state",()=>{render(<RevenueForecastingChart/>);expect(screen.getByText(/no chart data/i)).toBeInTheDocument();});
