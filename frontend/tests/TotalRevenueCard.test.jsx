import{render,screen}from"@testing-library/react";import{expect,it}from"vitest";import TotalRevenueCard from"../components/TotalRevenueCard";it("formats numeric revenue",()=>{render(<TotalRevenueCard value={50000}/>);expect(screen.getByText("₱50,000.00")).toBeInTheDocument();});it("defaults to zero",()=>{render(<TotalRevenueCard/>);expect(screen.getByText("₱0.00")).toBeInTheDocument();});
