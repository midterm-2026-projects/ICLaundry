import{render,screen}from"@testing-library/react";import{expect,it}from"vitest";import NetProfitCard from"../components/NetProfitCard";it("formats numeric net profit",()=>{render(<NetProfitCard value={20000}/>);expect(screen.getByText("₱20,000.00")).toBeInTheDocument();});it("defaults to zero",()=>{render(<NetProfitCard/>);expect(screen.getByText("₱0.00")).toBeInTheDocument();});
