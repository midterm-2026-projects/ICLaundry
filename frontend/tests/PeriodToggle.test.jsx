import{fireEvent,render,screen}from"@testing-library/react";import{expect,it,vi}from"vitest";import PeriodToggle from"../components/PeriodToggle";
it("renders all periods and selects the current one",()=>{render(<PeriodToggle selectedPeriod="weekly"/>);expect(screen.getByRole("button",{name:"Weekly"})).toHaveClass("active");expect(screen.getByRole("button",{name:"Monthly"})).toBeInTheDocument();expect(screen.getByRole("button",{name:"Yearly"})).toBeInTheDocument();});it("reports period changes",()=>{const change=vi.fn();render(<PeriodToggle onPeriodChange={change}/>);fireEvent.click(screen.getByRole("button",{name:"Yearly"}));expect(change).toHaveBeenCalledWith("yearly");});
