import { fireEvent, render, screen } from "@testing-library/react"; import { expect, it, vi } from "vitest"; import BranchFilter from "../components/BranchFilter";
it("renders branches and reports selection",()=>{const change=vi.fn();render(<BranchFilter selectedBranch="" onBranchChange={change} branches={[{id:"1",branch_name:"Main Branch"}]}/>);expect(screen.getByRole("option",{name:"Main Branch"})).toBeInTheDocument();fireEvent.change(screen.getByLabelText("Branch"),{target:{value:"1"}});expect(change).toHaveBeenCalledWith("1");});
it("renders the all branches default",()=>{render(<BranchFilter/>);expect(screen.getByLabelText("Branch")).toHaveValue("");});
