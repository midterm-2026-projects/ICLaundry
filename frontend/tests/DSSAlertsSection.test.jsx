import { render,screen } from "@testing-library/react";import{expect,it}from"vitest";import DSSAlertsSection from"../components/DSSAlertsSection";
it("renders structured alerts",()=>{render(<DSSAlertsSection alerts={[{title:"Low stock",category:"Inventory",severity:"Warning",description:"Restock now"}]}/>);expect(screen.getByText("Low stock")).toBeInTheDocument();expect(screen.getByText("Restock now")).toBeInTheDocument();});it("renders the empty state",()=>{render(<DSSAlertsSection/>);expect(screen.getByText(/no active alerts/i)).toBeInTheDocument();});
