import { render,screen } from "@testing-library/react";import{expect,it}from"vitest";import DSSRecommendationSection from"../components/DSSRecommendationSection";
it("renders structured recommendations",()=>{render(<DSSRecommendationSection recommendations={[{title:"Increase capacity",category:"Operations",priority:"High",description:"Add a washer"}]}/>);expect(screen.getByText("Increase capacity")).toBeInTheDocument();expect(screen.getByText("Add a washer")).toBeInTheDocument();});it("renders the empty state",()=>{render(<DSSRecommendationSection/>);expect(screen.getByText(/no recommendations/i)).toBeInTheDocument();});
